

def encrypt_aes(buffer):
	return buffer

def decrypt_aes(buffer):
	return buffer

class CipherRSA:
	def __init__(self, publicKey, privateKey):
		self.publicKey = publicKey
		self.privateKey = privateKey

	def encrypt_rsa(buffer):
		return buffer

	def decrypt_rsa(buffer):
		return buffer
